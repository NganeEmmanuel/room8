package com.room8.userservice.enums;

public enum PartyHabits {
    FREQUENTLY, // Enjoys partying regularly
    OCCASIONALLY, // Parties now and then
    NEVER, // Prefers no partying at all
    HOSTS_PARTIES, // Likes to host parties at home
    ATTENDS_BUT_DOES_NOT_HOST // Likes to go out but doesn’t host
}

