package com.userauth.user_auth.enums;

public enum SmokingStatus {
    NEVER, // Has never smoked
    OCCASIONAL, // Smokes occasionally
    FREQUENT, // Smokes frequently
    QUIT, // Used to smoke but quit
    ONLY_OUTSIDE // Smokes but only outside
}

